# Tech

_Empty — run `/spec:constitution` to fill this in for this product._

Stack per sub-project, package manager, conventions, and the commands that verify a change
(keep `steering/preferences.md` §Commands in sync with what lands here).
